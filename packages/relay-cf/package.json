{
  "name": "@html2figma/relay-cf",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "html2figma direct-send 공개 릴레이 (Cloudflare Workers + Durable Objects)",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@html2figma/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.3"
  }
}
